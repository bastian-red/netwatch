# netwatch dependencies (versions pinned from the local install).
# ARP sweep and ICMP ping also require libpcap on the host
# (provided by the OS, e.g. apt install libpcap0.8). The TCP-connect probe
# mode and bandwidth sampling need neither libpcap nor root.
scapy==2.7.0
psutil==7.2.2
Flask==3.1.3
Flask-SocketIO==5.6.1
# Pulled in by Flask-SocketIO; pinned for reproducibility.
python-socketio==5.16.3
python-engineio==4.13.3
simple-websocket==1.1.0
bidict==0.23.1
